/* How-to-fix guidance for every rule code - static knowledge, deterministic,
   shown in the Rules page and the finding inspector. Never engine behavior. */
export const RULE_FIXES = {
  "UPF-001": "Correct the leading command name to a supported UPF command (e.g. create_power_domain, set_isolation). Check for typos, stray whitespace, or a Tcl alias before the real command.",
  "UPF-002": "Check the option spelling against the IEEE 1801 option set for this command. Remove unsupported options, and keep only legal flags (e.g. -domain, -elements, -applies_to).",
  "UPF-003": "Add the missing required argument. Example: create_power_domain pd_x -include_scope, or set_domain_supply_net pd_x -primary_power_net VDD -primary_ground_net VSS.",
  "UPF-004": "Align the declared upf_version with the features used, or remove the version declaration. Older versions do not support constructs such as supply sets and advanced strategies.",
  "UPF-005": "Rewrite the legacy form using the current IEEE 1801 syntax (e.g. replace deprecated isolation/location forms with set_isolation / set_level_shifter strategy form).",
  "UPF-006": "Fix the Tcl structure: balance every brace and bracket, and remove dangling backslash continuations. Validate with a Tcl parser before running UPF analysis.",
  "UPF-010": "Define the referenced supply before use: add create_supply_net / create_supply_port / create_supply_set, then connect it with connect_supply_net. Check spelling and hierarchy path.",
  "UPF-011": "Create the power domain with create_power_domain before referencing it, or correct the domain name in the referencing command.",
  "UPF-012": "Fix the instance or hierarchical path so it matches the design hierarchy (e.g. aon_wrapper/pmu). Verify the scope with set_scope and the netlist snapshot.",
  "UPF-013": "Remove the duplicate definition or rename one occurrence. Each domain, supply, switch, strategy and PST name must be defined exactly once.",
  "UPF-014": "Reorder the UPF so the defining command appears before the use, or load the defining file first (load_upf ordering).",
  "UPF-015": "Break the cycle: move the later definition earlier, or restructure so no domain/supply depends on itself transitively.",
  "UPF-016": "Point set_scope at a module or instance that exists in the design, or remove the invalid scope target.",
  "UPF-020": "Add set_domain_supply_net (or -primary_supply_set on create_power_domain) so every domain names its primary power and ground.",
  "UPF-021": "Remove an instance from one of the overlapping domains, or restructure the domain boundaries so each instance belongs to exactly one domain.",
  "UPF-022": "Connect the supply with connect_supply_net or include it in a create_supply_set, or remove the orphaned supply if it is unused.",
  "UPF-023": "Provide both power and ground in the supply set (create_supply_set -power ... -ground ...), or attach an existing supply set to the domain.",
  "UPF-024": "Make the supply connection consistent: the port, net and set must reference the same supply object with matching power/ground roles.",
  "UPF-025": "Remove the unused supply state, or reference it in the PST with add_pst_state so it is actually exercised.",
  "UPF-030": "Reference the declared state in the PST via add_pst_state, or remove the unused declaration with remove_port_state.",
  "UPF-031": "Declare the state first with add_port_state / add_supply_state, then reference it in the PST. Check the state name spelling and the supply order in create_pst -supplies.",
  "UPF-032": "Add a create_pst table with add_pst_state rows covering every legal supply combination for the design modes.",
  "UPF-033": "Remove the empty/unreachable PST state or correct its supply-state assignments so it can be entered from a real mode.",
  "UPF-034": "Merge the duplicate/overlapping states or make each state a distinct supply combination.",
  "UPF-035": "Declare the transition state with add_pst_state, or remove the transition from the PST if it is not a real mode.",
  "UPF-036": "Add isolation or level-shifter strategies conditioned on the PST: set_isolation ... -domain ..., set_isolation_control, set_level_shifter. Every power-down crossing needs a strategy.",
  "UPF-037": "Add an isolation strategy (and control) on the crossing before the domain powers down. See set_isolation / set_isolation_control.",
  "UPF-038": "Add the switchable net to the PST supplies (create_pst -supplies ... + add_pst_state), or add the missing port state (add_port_state ... off).",
  "UPF-040": "Point the isolation strategy at an always-on supply (an always-on power/ground net, not the gated one), or move isolation to the always-on domain side.",
  "UPF-041": "Move isolation placement out of the switchable domain: use -location parent/sibling or the always-on domain so isolation logic is not powered down.",
  "UPF-042": "Add set_isolation for the un-isolated crossing between the domains, with a clamp value and control signal.",
  "UPF-043": "Remove the redundant isolation strategy - the crossing is already isolated by an existing strategy.",
  "UPF-044": "Extend -applies_to to cover inputs and outputs (both) so all boundary ports of the domain are isolated.",
  "UPF-045": "Pair every set_isolation with a matching set_isolation_control (same strategy name), or remove the unmatched strategy.",
  "UPF-046": "Use a legal clamp value: 0, 1, latch or Z.",
  "UPF-047": "Drive the isolation control from an always-on source: move the control signal into the always-on domain or gate it with an always-on enable.",
  "UPF-050": "Power retention from an always-on supply: change retention_power_net to a supply that stays up in the off state, or move retention out of the gated domain.",
  "UPF-051": "Drive the retention save/restore controls from an always-on source so they are active while the domain is gated.",
  "UPF-052": "Extend set_retention -elements to cover the registers that must hold state, or add a matching retention strategy for the uncovered registers.",
  "UPF-053": "Connect the retention control to a real signal (not a constant 0/1), or remove the constant tie and drive save/restore from the PMU.",
  "UPF-054": "Add set_retention_control with save_signal and restore_signal for the retention strategy, or remove the retention if no control exists.",
  "UPF-060": "Remove the level shifter when the source and target voltages are equal, or verify the threshold makes the shifter necessary.",
  "UPF-061": "Add set_level_shifter on the crossing where voltages differ, with the correct -rule (low_to_high / high_to_low / both) and -applies_to.",
  "UPF-062": "Fix the level-shifter rule to match the actual voltage direction: low_to_high for a low-to-high crossing, high_to_low for the reverse.",
  "UPF-063": "Move level-shifter placement out of the switchable domain (-location self/parent/sibling) so the shifter is not powered down with the domain.",
  "UPF-064": "Drive the level-shifter enable from an always-on source, or move the control into the always-on domain.",
  "UPF-070": "Reference a defined supply in the switch: create/connect the input and output supply ports first, then re-declare the switch.",
  "UPF-071": "Drive the switch control from an always-on source, or confirm the control itself is never powered down.",
  "UPF-072": "Isolate the always-on signal at the switchable-domain boundary (set_isolation), or route it through the always-on domain.",
  "UPF-073": "Remove the unused switch or connect its output supply to a domain/strategy that consumes it.",
  "UPF-074": "Add an on_state with a Boolean condition tied to a real control port, or remove the state condition if it is not needed.",
  "UPF-080": "Fix the -elements instance path so it exists in the design (netlist snapshot), or remove the unknown instance.",
  "UPF-081": "Fix the control signal path so it resolves in the design, or remove the unknown signal reference.",
  "UPF-082": "Add an isolation/level-shifter strategy on the uncovered endpoint crossing, matching the voltage and power-down behavior of the two domains.",
  "UPF-083": "Extend the retention -elements list to cover the registers that must retain state, or add retention for the gapped register group.",
  "UPF-084": "Fix the library cell PG mapping so the cell power/ground pins match the domain supplies, or update the library snapshot.",
  "UPF-090": "Power the repeater from an always-on supply (repeater_power_net), or move the repeater out of the gated domain.",
  "UPF-091": "Drive the repeater enable from an always-on source, or move the control into the always-on domain.",
  "UPF-092": "Place the repeater outside the switchable domain (-location parent/sibling/self with an always-on supply) so it is not powered down.",
  "UPF-093": "Add -elements to the repeater strategy, or remove the empty repeater declaration.",
  "UPF-094": "Add set_repeater_control with the enable signal, or remove the repeater if no control exists.",
  "UPF-095": "Define the entity in the parent UPF before promoting it (upf_promote), or remove the promote of an undefined entity.",
  "UPF-096": "Provide the referenced design context so demotion can be verified, or remove the unverifiable demotion.",
  "UPF-097": "Verify the hierarchical composition: load and analyze the child UPF in the parent scope (set_scope / load_upf) so the references resolve.",
  "UPF-098": "Define the equivalent supply (create_supply_set / create_supply_net) referenced by the equivalence directive, or remove the directive.",
};

